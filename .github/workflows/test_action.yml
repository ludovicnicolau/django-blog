name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Launch tests
      run: docker compose -f compose.test.yml up --build --abort-on-container-exit
